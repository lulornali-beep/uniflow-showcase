# ⚡ 快速测试指南

## 🚀 快速启动

```bash
cd admin-console
npm run dev
```

访问：http://localhost:3000

---

## ✅ 快速测试清单

### 1️⃣ 检查开发服务器

- [x] 开发服务器已启动
- [ ] 访问 http://localhost:3000 可以看到页面

### 2️⃣ 环境变量检查

**需要确保 `.env.local` 文件存在并包含：**

```env
NEXT_PUBLIC_SUPABASE_URL=https://civlywqsdzzrvsutlrxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

**检查命令**：
```bash
cd admin-console
cat .env.local
```

**如果没有文件，需要创建**：
```bash
cd admin-console
cat > .env.local << 'EOF'
NEXT_PUBLIC_SUPABASE_URL=https://civlywqsdzzrvsutlrxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImNpdmx5d3FzZHp6cnZzdXRscnh4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjQ3NTkzODUsImV4cCI6MjA4MDMzNTM4NX0.vHueW-6OoZg1srGLzMvRGS1Cwy1bpyX-isVtJ_z6SbQ
EOF
```

### 3️⃣ 页面功能测试

#### 登录页面 (`/login`)

1. **访问** http://localhost:3000
   - 应该自动重定向到 `/login`
   - 看到登录表单

2. **UI 检查**
   - [ ] 看到 "UniFlow 智汇流" 标题
   - [ ] 看到邮箱输入框
   - [ ] 看到密码输入框
   - [ ] 看到登录按钮（紫色）

#### Dashboard 页面 (`/dashboard`)

**注意**：需要先在 Supabase 创建测试用户才能登录。

**创建测试用户**：
1. 访问 Supabase Dashboard
2. Authentication → Users → Add user
3. 输入邮箱和密码（例如：`admin@test.com` / `test123456`）

**登录后检查**：
- [ ] 看到深色侧边栏（左侧）
- [ ] 看到 "UniFlow 智汇流" 标题
- [ ] 看到导航菜单（数据看板、AI 智能采集台、活动管理、设置）
- [ ] 看到顶部导航栏（白色）
- [ ] 看到用户邮箱显示
- [ ] 看到 "数据看板" 页面内容
- [ ] 看到 4 个数据卡片（占位符）

---

## 🎯 测试重点

### ✅ 已完成的功能

1. **项目初始化** ✅
   - Next.js 14 项目已创建
   - TypeScript 配置完成
   - Tailwind CSS 配置完成

2. **基础依赖** ✅
   - Supabase 客户端库已安装
   - Lucide React 图标库已安装
   - 工具库已安装

3. **认证系统** ✅
   - 登录页面已实现
   - 认证中间件已配置
   - 路由保护已实现

4. **基础布局** ✅
   - Sidebar 组件已实现
   - TopNav 组件已实现
   - Dashboard 布局已实现

### 🔲 待测试的功能

1. **登录功能**
   - [ ] 使用有效凭据登录
   - [ ] 使用无效凭据登录（错误提示）
   - [ ] 退出登录功能

2. **路由保护**
   - [ ] 未登录访问 `/dashboard` 应重定向到 `/login`
   - [ ] 已登录访问 `/login` 应重定向到 `/dashboard`

3. **导航功能**
   - [ ] 点击侧边栏菜单项切换页面
   - [ ] 当前页面菜单项高亮显示

---

## 📝 测试结果

**测试日期**：_____________  
**测试状态**：□ 通过  □ 部分通过  □ 未通过

**发现的问题**：
1. 
2. 
3. 

**备注**：
- 

---

## 🆘 遇到问题？

查看详细测试指南：`TEST_GUIDE.md`

